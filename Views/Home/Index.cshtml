@model HojaDeVida.Models.ResumeViewModel

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hoja de Vida C#</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(to bottom, #ffcccc, #ff9999);
            color: #333;
            padding: 20px;
        }

        /* .photo {
            width: @Model.PhotoSize px;
            height: auto; */

        .container {
            width: 80%;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .photo {
            width: @Model.PhotoSize%;
            border-radius: 50%;
            margin-bottom: 20px;
        }

        .details, .experience {
            margin-bottom: 20px;
        }

        h1, h2 {
            color: #ff3333;
        }

        .experience ul {
            list-style-type: none;
            padding-left: 0;
        }

        .experience ul li {
            background-color: #f9f9f9;
            margin: 5px 0;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #ddd;
        }

        .btn {
            padding: 10px 20px;
            background-color: #ff6666;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .btn:hover {
            background-color: #ff33ff;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>@Model.Name</h1>
        <img src="@Model.PhotoUrl" alt="Photo" class="photo">
        <div class="details">
            <p><strong>Fecha de Nacimiento:</strong> @Model.BirthDate.ToShortDateString()</p>
        </div>
        <!-- Botones para cambiar el tamaño de la foto -->
        <button onclick="changePhotoSize(-10)">Redicir tamaño</button>
        <button onclick="changePhotoSize(10)">Incrementar tamaño</button>
        <script>
            function changePhotoSize(delta) {
                var img = document.querySelector('.photo');
                var currentSize = img.style.width ? parseInt(img.style.width) : 50; // Tamaño por defecto
                img.style.width = (currentSize + delta) + 'px';
            }
        </script>

        <div class="experience">
            <h2>Experiencia</h2>
            <ul>
                @foreach (var experience in Model.Experiences)
                {
                    <li>@experience</li>
                }
            </ul>
            <!-- Formularios para agregar experiencia -->
            <form asp-action="AddExperience" method="post">
                <input type="text" name="newExperience" placeholder="Agregar nueva experiencia">
                <button type="submit" class="btn">Agregar Experiencia</button>
            </form>
        </div>
    </div>
</body>
</html>